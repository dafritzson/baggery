{
  "name": "baggery",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && cd app && tsc --noEmit",
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "test:int": "supabase db reset && INTEGRATION=1 vitest run"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.117.1",
    "@types/node": "^22.20.4",
    "supabase": "^2.117.0",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
